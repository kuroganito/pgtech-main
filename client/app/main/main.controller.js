'use strict';

(function() {
  class MainController {
  }

  angular.module('pgtechApp')
    .component('main', {
      templateUrl: 'app/main/main.html',
      controller: MainController
    });
})();
